{
  "_comment": "What this file is: minimal package.json for repo scripts. Edit by maintainers. Update when scripts or package metadata change. Approvals: Eng Lead.",
  "name": "design-system-template",
  "version": "0.0.0",
  "description": "Template repo for a production-ready design system (tokens, components, docs, CI).",
  "private": true,
  "scripts": {
    "test": "echo \"No tests configured yet\" && exit 0",
    "lint": "echo \"No linter configured yet\"",
    "build": "echo \"No build configured for template\"",
    "create-release-draft": "node scripts/create-release-draft.js --help"
  },
  "author": "",
  "license": "MIT"
}

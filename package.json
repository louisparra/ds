{
  "_comment": "UPDATE: ensure token validation script is present. Edit by Maintainers; update when scripts change; approvals: Eng Lead.",
  "name": "design-system-template",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "prepare": "husky install",
    "bootstrap": "npm ci",
    "build:packages": "bash scripts/build-packages.sh",
    "test": "echo \"No tests configured yet\" && exit 0",
    "lint": "eslint . --ext .js,.mjs,.cjs",
    "format": "prettier --write \"**/*.{js,json,md,css,scss,html,mdx}\"",
    "format:check": "prettier --check \"**/*.{js,json,md,css,scss,html,mdx}\"",
    "build": "echo \"No root build configured\"",
    "create-release-draft": "node scripts/create-release-draft.js --help",
    "token-validate": "node scripts/token-validate.js",
    "token-lint:local": "node linting/token-lint-rules.js --mode=local",
    "token-lint:ci": "node linting/token-lint-rules.js --mode=ci",
    "build-storybook": "node scripts/build-storybook.js",
    "build-tokens": "node scripts/build-tokens.js",
    "build-tokens:sd": "node scripts/build-tokens-sd.js",
    "publish-packages": "bash scripts/publish-packages.sh"
  },
  "devDependencies": {
    "@commitlint/cli": "^17.6.7",
    "@commitlint/config-conventional": "^17.6.7",
    "ajv": "^8.12.0",
    "ajv-errors": "^3.0.0",
    "eslint": "^8.45.0",
    "husky": "^8.0.3",
    "lint-staged": "^13.2.2",
    "prettier": "^2.8.8",
    "style-dictionary": "^3.1.0"
  },
  "lint-staged": {
    "*.{js,mjs,cjs}": [
      "eslint --fix",
      "prettier --write"
    ],
    "*.{json,md,css,scss,html,mdx}": [
      "prettier --write"
    ]
  },
  "overrides": {
    "micromatch": "^4.0.8"
  }
}
